services:
  api:
    build:
      context: ../services/api
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - ../.env
    environment:
      - PORT=3000
      - NODE_ENV=development
      - WEB_ORIGIN=http://localhost:4321
